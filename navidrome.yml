networks:
  proxy:

volumes:
  navidrome-data:

services:
  navidrome:
    image: ghcr.io/navidrome/navidrome:0.60.3
    environment:
      - ND_SCANSCHEDULE=@daily
      - ND_ENABLETRANSCODINGCONFIG=false
      # data collection: https://www.navidrome.org/docs/getting-started/insights/
      - ND_ENABLEINSIGHTSCOLLECTOR=true
      # used to fetch artist images
      - ND_LASTFM_ENABLED=true
      - ND_LASTFM_APIKEY=${NAVIDROME_LASTFM_APIKEY}
      - ND_LASTFM_SECRET=${NAVIDROME_LASTFM_SECRET}
      # todo: enable external auth when it supports OIDC, ForwardAuth is a pain
      # see https://www.navidrome.org/docs/usage/integration/authentication/
      # - ND_EXTAUTH_TRUSTEDSOURCES=172.16.0.0/16
      # - ND_EXTAUTH_USERHEADER=Remote-User
      # - ND_ENABLEUSEREDITING=false
    expose:
      - 4533 # webui
    extends:
      file: common.yml
      service: log-to-json
    networks:
      - proxy
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - navidrome-data:/data
      - ${MEDIA_COLLECTION_MUSIC_PATH}:/music:ro
