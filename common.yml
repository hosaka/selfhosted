version: "3"

services:
  log-to-json:
    logging:
      driver: json-file
      options:
        max-size: 100m
        max-file: "3"
        tag: "{{.ImageName}}|{{.Name}}|{{.ImageFullID}}|{{.FullID}}"
