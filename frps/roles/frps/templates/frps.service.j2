[Unit]
Description = frp server
After = network.target syslog.target
Wants = network.target

[Service]
Type = simple
ExecStart = {{ frps_bin }} -c {{ frps_config_path }}

[Install]
WantedBy = multi-user.target
